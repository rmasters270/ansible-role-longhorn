galaxy_info:
  author: rmasters270
  description: Install Longhorn Helm Chart
  company: none
  license: MIT
  min_ansible_version: '2.1'

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: [kubernetes, k3s, helm, longhorn]

dependencies:
  - role: rmasters270.helm
